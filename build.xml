<?xml version="1.0"?>
<project name="jsf" default="run">
    <property file="build.properties"/>
    <path id = "libs">
        <fileset dir="${lib.dir}">
            <include name="**/*.jar"/>
        </fileset>
    </path>

    <target name="compile">
        <mkdir dir="${classes.dir}"/>
        <javac srcdir = "${src.dir}" destdir="${classes.dir}" classpathref="libs"/>
    </target>
    <target name="run" depends="compile">
        <java classname="HelloWorld" classpath="build/classes"/>
    </target>
    <target name = "clean">
        <delete dir = "${build.dir}"/>
    </target>
    <target name = "xml">
        <xmlvalidate failonerror="no" lenient="yes" warn="yes"
                     classname="org.apache.xerces.parsers.SAXParser"
            classpath="${lib.dir}/${xerces}">
            <fileset dir="${xml.dir}" includes="**/*.xml"/>
        </xmlvalidate>
    </target>
    <target name = "scp" depends="build">
        <scp file="" todir="${scp.dir}" password="${scp.password}" port="${scp.port}" />
    </target>
</project>